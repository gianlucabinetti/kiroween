/**
 * Mock Database for Demo
 * In production, this would be a real Prisma client
 * For now, apps use their own mock databases
 */

// Placeholder - apps should use their own db
export const db = {
  // This is intentionally empty
  // Each app (haunted-tasks, coven-crm) has its own mock db
}
